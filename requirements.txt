# Core PDF processing (multiple backends with fallback)
PyMuPDF>=1.21.0      # Preferred backend - fast and reliable
pdfplumber>=0.7.0    # Alternative backend - good for tables
PyPDF2>=3.0.0        # Fallback backend - basic functionality

# CLI interface and display
click>=8.0.0         # Command-line interface framework
rich>=12.0.0         # Beautiful terminal output

# Data and configuration formats  
PyYAML>=6.0          # YAML support for configs and output
tabulate>=0.9.0      # Table formatting

# Optional OCR support (install separately if needed)
# pytesseract>=0.3.9   # OCR for scanned PDFs
# Pillow>=8.0.0        # Image processing for OCR